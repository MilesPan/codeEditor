name: GithubDockerAction
run-name: 🚀 ${{ github.actor }} is Running GithubDockerAction 🚀
# docker pull pdy666/codeeditor-client:latest
# docker pull pdy666/codeeditor-server:latest
# docker-compose -f /www/server/panel/data/compose/test1/template/test1.yaml down
# docker-compose -f /www/server/panel/data/compose/test1/template/test1.yaml up -d
on:
  push:
    branches: [master]

jobs:
  build:
    runs-on: ubuntu-latest
    steps:
      - name: Checkout code
        uses: actions/checkout@v3

      # - name: Setup QEMU
      #   uses: docker/setup-qemu-action@v3

      # - name: Setup Buildx
      #   uses: docker/setup-buildx-action@v3

      # - name: Docker login
      #   uses: docker/login-action@v3
      #   with:
      #     username: '${{secrets.DOCKERHUB_USERNAME}}'
      #     password: '${{secrets.DOCKERHUB_PASSWORD}}'

      # - name: 编译+推送Client镜像
      #   uses: docker/build-push-action@v3
      #   with:
      #     tags: pdy666/codeeditor-client:latest
      #     push: true
      #     file: Client.Dockerfile
      #     cache-from: type=registry,ref=pdy666/codeeditor-client:latest
      #     cache-to: type=registry,ref=pdy666/codeeditor-client:latest,mode=max

      # - name: 编译+推送Server镜像
      #   uses: docker/build-push-action@v3
      #   with:
      #     tags: pdy666/codeeditor-server:latest
      #     push: true
      #     file: Server.Dockerfile
      #     cache-from: type=registry,ref=pdy666/codeeditor-server:latest
      #     cache-to: type=registry,ref=pdy666/codeeditor-server:latest,mode=max

      - name: 更新最新镜像
        uses: appleboy/ssh-action@v1.0.3
        with:
          host: ${{ secrets.SSH_HOST }}
          username: ${{ secrets.SSH_USERNAME }}
          password: ${{ secrets.SSH_PASSWORD }}
          port: ${{ secrets.SSH_PORT }}
          script: |
            scp -o StrictHostKeyChecking=no ./docker-compose.yml ${{secrets.SSH_USERNAME}}@${{secrets.SSH_HOST}}:/www/server/panel/data/compose/