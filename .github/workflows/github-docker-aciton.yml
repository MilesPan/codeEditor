name: GithubDockerAction
run-name: 🚀 ${{ github.actor }} is Running GithubDockerAction 🚀
on:
  push:
    branches: [master, workflow_dispatch]

jobs:
  build:
    runs-on: ubuntu-latest
    steps:
      - name: Setup QEMU
        uses: docker/setup-qemu-action@v3
      
      - name: Setup Buildx
        uses: docker/setup-buildx-action@v3
      
      # - name: Docker login
      #   uses: docker/login-action@v3
      #   with:
      #     username: "${{secrets.DOCKERHUB_USERNAME}}"
      #     password: "${{secrets.DOCKERHUB_PASSWORD}}"
      
      - name: 编译Client镜像
        run: |
          docker buildx build -t pdy666/codeeditor-client:latest -f ./Client.Dockerfile
      - name: 推送Client镜像
        run: |
          echo ${{secrets.DOCKERHUB_PASSWORD}} | docker login -u ${{secrets.DOCKERHUB_USERNAME}} --password-stdin
          docker push pdy666/codeeditor-client:latest
          # .
      #   with:
      #     context: .
      #     file: ./Client.Dockerfile
      #     push: true
      #     tags: pdy666/codeeditor-client:latest
      # - name: 编译Server镜像
      #   uses: docker/build-push-action@v6
      #   with:
      #     context: .
      #     file: ./Server.Dockerfile
      #     push: true
      #     tags: pdy666/codeeditor-server:latest